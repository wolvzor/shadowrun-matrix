plugins {
	id "java"
	id "scala"
	id "com.github.johnrengelman.shadow" version "1.2.3"
	id "idea"
  id "jacoco"
}

dependencies {
	compile (
		"org.scalactic:scalactic_${libVersions.scalaMinor}:${libVersions.scalatest}",
	)
}